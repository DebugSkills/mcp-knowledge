# Docker volumes (runtime)
data/

# Модели (тяжёлые)
models_cache/

# Python
__pycache__/
*.pyc
.venv/
*.egg-info/

# Coverage artifacts (generated)
.coverage
mcp_server/.coverage
htmlcov/

# Air-gap deploy bundle (пересобирается: make bundle)
artifacts/
mcp-kb-airgap-bundle.tar.gz
.coverage.*

# Env (секреты)
.env
!.env.example

# IDE
.vscode/
.idea/

# Agent backups
.backup/

# Operator scratch notes (локальные, не для публичной репы)
NOTES.md

# SSOT Knowledge Base (auto-gen, подключается через symlink)
.knowledge/
.roomodes
.kilo/
# AGENTS.md — рукописная публичная инструкция (создание скиллов), НЕ игнорируем
.board.md
.boardData.md
.roo/
.knowledge
plans
.venv/

# Throwaway artifacts (реверсивное удаление)
.trash/
.playwright-mcp/

# Ansible vault (секреты; пример — vault.yml.example)
ansible/inventory/group_vars/all/vault.yml

# Внутренние контентные материалы (НЕ для публичной репы)
docs/mcp-perspektivy-slide.md
docs/mcp-perspektivy-slide.pdf
docs/pedagogicheskiy-algoritm-nauchnogo-issledovaniya-ksa.md
docs/pedagogicheskiy-algoritm-nauchnogo-issledovaniya-ksa.pdf
