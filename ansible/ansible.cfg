[defaults]
inventory = inventory/hosts.yml
roles_path = roles
forks = 2
timeout = 120
host_key_checking = accept-new
interpreter_python = auto_silent
# vault_password_file = ~/.config/mcp-kb/vault-pass   # раскомментирует оператор
