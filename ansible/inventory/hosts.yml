all:
  children:
    mcp_kb_old:
      hosts:
        mcp-knowledge-old:
          ansible_host: <old-server-ip>   # TODO: текущий хост
          ansible_user: root
    mcp_kb_new:
      hosts:
        mcp-knowledge-new:
          ansible_host: <new-server-ip>   # TODO: целевой хост
          ansible_user: root
