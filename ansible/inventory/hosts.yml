all:
  children:
    mcp_kb_old:
      hosts:
        mcp-knowledge-old:
          ansible_host: <old-server-ip>   # TODO: текущий хост (нужен только для Сценария C — перенос old→new)
          ansible_user: root
    mcp_kb_new:
      hosts:
        aikb:
          # Целевой хост БЗ. Параметры подключения и переменные ролей — в inventory/host_vars/aikb.yml
