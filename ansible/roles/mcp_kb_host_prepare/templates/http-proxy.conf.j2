# Managed by Ansible — role mcp_kb_host_prepare
[Service]
Environment="HTTP_PROXY={{ mcp_kb_host_prepare__proxy }}"
Environment="HTTPS_PROXY={{ mcp_kb_host_prepare__proxy }}"
Environment="NO_PROXY={{ mcp_kb_host_prepare__no_proxy }}"
