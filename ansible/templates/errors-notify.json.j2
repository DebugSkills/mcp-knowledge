{
  "_doc": "TG-доставка weekly-отчёта Error→Rule (Ф4, P2-10; ansible-managed): bot_token из ansible-vault, режим 0600, НИКОГДА не в git и не логируется. errors_report.py --send-tg читает; пустые значения → 'TG: skip', exit 0 (graceful).",
  "bot_token": "{{ vault_telegram_bot_token | default('') }}",
  "chat_id": "{{ vault_telegram_chat_id | default('') }}"
}
